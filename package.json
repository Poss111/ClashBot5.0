{
  "name": "clashbot5",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "infra",
    "services"
  ],
  "scripts": {
    "bootstrap": "cd infra && npm install",
    "lint": "echo \"add linters\"",
    "deploy:ui": "pushd frontend && flutter build web && popd && aws s3 sync frontend/build/web s3://$FRONTEND_BUCKET --delete --profile PowerUserAccess-816923827429"
  }
}