{
  "name": "clashbot5",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "infra",
    "services"
  ],
  "scripts": {
    "bootstrap": "cd infra && npm install",
    "lint": "echo \"add linters\"",
    "deploy:ui": "cd frontend && flutter build web && aws s3 sync frontend/build/web s3://$FRONTEND_BUCKET --delete"
  }
}